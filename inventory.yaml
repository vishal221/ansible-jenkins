all:
  children:
    jenkins:
      hosts:
        172.31.12.135:
  vars:
    ansible_user: ubuntu
    ansible_ssh_private_key_file: "~/.ssh/privatekey"
    ansible_ssh_common_args: "-o StrictHostKeyChecking=no"
